name: Django CI/CD

on:
  push:
    branches: [action]
  pull_request:
    branches: [action]

jobs:
  build:
    runs-on: ubuntu-latest
    container: python:3.11.9-slim

    services:
      mysql:
        image: mysql:latest
        ports:
          - 3306:3306
        env:
          MYSQL_DATABASE: django_db
          MYSQL_PASSWORD: root
          MYSQL_ROOT_PASSWORD: root
        options: >-
          --health-cmd "mysqladmin ping -h localhost -u root --password=$MYSQL_ROOT_PASSWORD"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      minio:
        image: lazybit/minio
        ports:
          - 9000:9000
        env:
          MINIO_ROOT_USER: minio
          MINIO_ROOT_PASSWORD: minio123
        options: >-
          --name=minio
          --health-cmd "curl -f http://localhost:9000/minio/health/live || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      # Checkout the code from the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Docker Buildx for building multi-architecture Docker images
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Cache Docker layers to speed up the build process
      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v2

      # test the Django application use python
      - name: Test Django application
        run: |
          pip install -r backend/requirements.txt
          python backend/manage.py test
        env:
          DATABASE_URL: mysql://root:root@localhost:3306/django_db
          MINIO_ACCESS_KEY: minio
          MINIO_SECRET_KEY: minio123

      # Build and push the Docker image to Docker Hub

    # steps:
    #   - name: Checkout code
    #     uses: actions/checkout@v3

    #   - name: Set up Docker Buildx
    #     uses: docker/setup-buildx-action@v2

    #   - name: Cache Docker layers
    #     uses: actions/cache@v3
    #     with:
    #       path: /tmp/.buildx-cache
    #       key: ${{ runner.os }}-buildx-${{ github.sha }}
    #       restore-keys: |
    #         ${{ runner.os }}-buildx-

    #   - name: Set up QEMU
    #     uses: docker/setup-qemu-action@v2

    #   - name: Log in to Docker Hub
    #     uses: docker/login-action@v2
    #     with:
    #       username: ${{ secrets.DOCKER_USERNAME }}
    #       password: ${{ secrets.DOCKER_PASSWORD }}

    #   - name: Build and push Docker image
    #     id: docker_build
    #     uses: docker/build-push-action@v5
    #     with:
    #       context: .
    #       file: backend/Dockerfile
    #       push: true
    #       tags: ${{ secrets.DOCKER_USERNAME }}/django-app:${{ github.sha }}

    #   - name: Run tests
    #     run: |
    #       docker run --rm \
    #       --network host \
    #       -e DATABASE_URL=mysql://root:root@localhost:3306/django_db \
    #       -e MINIO_ACCESS_KEY=minio \
    #       -e MINIO_SECRET_KEY=minio123 \
    #       ${{ secrets.DOCKER_USERNAME }}/django-app:${{ github.sha }} \
    #       python backend/manage.py test

    #   - name: Create GitHub Release
    #     id: create_release
    #     uses: actions/create-release@v1
    #     env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     with:
    #       tag_name: ${{ github.sha }}
    #       release_name: Release ${{ github.sha }}
    #       draft: false
    #       prerelease: false

    #   - name: Upload Docker image to release
    #     uses: actions/upload-artifact@v3
    #     with:
    #       name: django-docker-image
    #       path: ./django-docker-image.tar

    #   - name: Save Docker image to file
    #     run: docker save ${{ secrets.DOCKER_USERNAME }}/django-app:${{ github.sha }} -o ./django-docker-image.tar
